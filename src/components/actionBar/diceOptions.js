import { Button, ButtonGroup } from '@material-ui/core';

export default function DiceOptions() {

    return (<ButtonGroup>
        <Button>1 dice</Button>
        <Button>2 dice</Button>
        </ButtonGroup>
    );
}