import React from 'react';

import Lobby from '../components/lobby'

const LobbyPage = () => {
    return (
        <>
            <div>Lobby</div>
            < Lobby ></Lobby >
        </>
    );
};

export default LobbyPage;
